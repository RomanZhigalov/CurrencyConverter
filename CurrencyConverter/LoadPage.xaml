<Page
    x:Class="CurrencyConverter.LoadPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CurrencyConverter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" d:DesignWidth="800" d:DesignHeight="1000">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60*"/>
            <RowDefinition Height="933*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Конвертер валют" FontSize="30" 
                   FontFamily="Cambria Math" Foreground="#FFC1C1C1" 
                   HorizontalAlignment="Center" VerticalAlignment="Center" 
                   Height="35" Width="237" >
            <TextBlock.FocusVisualPrimaryBrush>
                <SolidColorBrush Color="{ThemeResource SystemAccentColor}"/>
            </TextBlock.FocusVisualPrimaryBrush>
        </TextBlock>
        <ProgressRing Name="splashProgressRing" Grid.Row="1" 
                      IsActive="True" Width="100" Height="100" 
                      HorizontalAlignment="Center"/>
    </Grid>
</Page>
